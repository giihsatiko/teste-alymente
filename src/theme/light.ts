import { Sizes } from "./sizes";
import { ThemeType } from "./types";

export const lightTheme:ThemeType = {
  palette: {
    background: '#ffffff',
    border: '#555555',
    input: '#ffffff',
    text: {
      primary: '#000000',
      secondary: '#555555',
    },
  },
  sizes: Sizes,
}